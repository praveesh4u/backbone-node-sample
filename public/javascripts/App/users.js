(function() {
  var JSONRenderer, express, router;

  express = require('express');

  router = express.Router();

  JSONRenderer = require('../lib/jsonRenderer');


  /* GET home page. */

}).call(this);
